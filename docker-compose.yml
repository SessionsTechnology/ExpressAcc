services:
  express-acc:
    build: .
    image: expressacc:latest
    environment:
      TZ: ${TZ:-America/Chicago}
      DATABASE_FILE: /data/db.json
    ports:
      - "${PORT:-3001}:3001"
    volumes:
      - express-acc-data:/data
    restart: unless-stopped

volumes:
  express-acc-data:
